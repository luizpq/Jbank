spring.application.name=BancoLuiz

# URL de conex�o (Endere�o do cofre)
spring.datasource.url=jdbc:postgresql://localhost:5432/jbank_db

# Usu�rio padr�o do Postgres
spring.datasource.username=postgres

# A senha que voc� definiu no ALTER USER
spring.datasource.password=SUA_SENHA_AQUI

# O "Tradutor" (Driver)
spring.datasource.driver-class-name=org.postgresql.Driver

# Oculta as verifica��es do Spring
logging.level.root=WARN
